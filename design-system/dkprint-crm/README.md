# DKPrint CRM design system

Canonical tokens for the product (TZ §17): background `#f3f7f9`, primary `#0891B2`, CTA `#22C55E`.

Use `MASTER.md` + `styles.css` as the UI reference. App shell may re-declare CSS variables in `src/app/globals.css`.
